#include <bits/stdc++.h>
using namespace std;

int countPrimes(int strt,int end)
{
   
   }
   return count;
}
int main(){
   int START, END;
   cin>>START>>END;
   cout <<endl<<"Primes in Ranges : "<<countPrimes(START,END);
   return 0;
}
